作业1：
1、font和center为什么自己加了删除线
2、如何用class定义同一行字不用样式，

第一次百度使用了这种方法:
<font color="red" size="5px">第一行字</font><font color="lightblue">第一行字</font>
作业中样式可以正常显示，但是更换style方法后，
<font style="font-size: 20px">Android</font>

发现文字大小使用同样的px值，显示大小差距很大（疑惑点）

3、如何使文字居中，使用center标签是所有纵列文字在整个页面中垂直居中，如何实现在自己想要的固定位置左对齐居中。

4、想要一些文字加粗只能每个词都加b标签吗？调整大小也是

5、如何把提交按钮变大

作业2：
为什么合并数值输入了三列显示出来是两列，第6个框还自动加宽了